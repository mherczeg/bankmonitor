# Committed so a fresh clone type-checks without running a generator first, and
# regenerated by `npm run build` so it can never be silently stale.
src/routeTree.gen.ts linguist-generated=true -diff

# Generated too, but deliberately not marked as such: `linguist-generated` is what
# collapses a file's diff in a pull request, and this one's diff is the backend's API
# moving, which is the thing a reviewer most has to read.
src/api/schema.gen.ts -linguist-generated
